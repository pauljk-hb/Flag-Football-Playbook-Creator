{
  "name": "@playbook/core",
  "version": "1.0.0",
  "type": "module",
  "description": "Die Core-Bibliothek für das Playbook-Projekt.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch -p tsconfig.build.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "fabric": "^7.4.0"
  },
  "devDependencies": {
    "@types/fabric": "^5.3.11",
    "@vitest/coverage-v8": "^4.1.10",
    "@vitest/ui": "^4.1.10",
    "typescript": "^7.0.2"
  }
}
